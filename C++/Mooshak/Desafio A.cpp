#include <iostream>
#include <string>
#include <locale.h>
#include <cstdlib>
#include <time.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Portuguese");
	int A, L, C, V;
	cin >> A;
	cin >> L;
	cin >> C;
	V = C * L * A;
	if (A=>3 && V=>50)

	return 0;
}